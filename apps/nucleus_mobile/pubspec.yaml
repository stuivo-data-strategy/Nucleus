name: nucleus_mobile
description: "Nucleus PBAC Expenses — Adaptive Flutter App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.4.0
  google_fonts: ^6.2.1
  provider: ^6.1.5
  fl_chart: ^0.70.2
  image_picker: ^1.1.2
  intl: ^0.20.2
  shimmer: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
